mode: enforce
interface: eth0
dns:
  listen: 127.0.0.1:1053
  upstreams:
    - 1.1.1.1:53
  mark: 4242
trust:
  max_stale_hold: 0s
  sweep_interval: 30s
policy:
  default: deny
  allow:
    - "*.github.com"
    - github.com
